{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "../dist",
    // [LAW:one-source-of-truth] keep the build cache out of the published dist/.
    "tsBuildInfoFile": "./tsconfig.tsbuildinfo",
    "rootDir": ".",
    "types": ["node"]
  },
  "include": ["./*.ts", "./sinks/**/*.ts", "./commands/**/*.ts"],
  "references": [
    { "path": "protocol" },
    { "path": "transport" }
  ]
}
